def format_apa(author, year, title, publisher):
    return f"{author} ({year}). *{title}*. {publisher}."

def format_mla(author, title, publisher, year):
    return f"{author}. *{title}*. {publisher}, {year}."
